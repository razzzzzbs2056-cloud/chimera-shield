<!-- GENERATED from config/catalog.yaml by `python -m src.cli generate`. Do not edit by hand. -->

# Subagent 03B — Banking and Credit Analyst

**Parent agent:** 03 — Monetary Policy and Banking
**Depends on:** 20A, 20B, 20C, 20D, 20E

## Instructions

Study banking resilience, nonperforming loans, financial inclusion and productive credit allocation.

Answer these research questions:

1. What are the system NPL ratio, capital adequacy and sectoral credit concentration?
2. How much private credit flows to productive sectors versus real estate and trade?
3. What is the account-ownership and borrowing gap by province and gender?

## Evidence requirements

**Data required:**
- NPL ratio
- CAR
- credit by sector
- Findex indicators

**Sources (priority order; see research/sources/source_catalog.yaml):**
- nrb
- imf_article_iv
- wb_wdi

**Rules:**
- Retrieve primary documents; record each value via ObservationStore with raw-file hash.
- Report publication date and data period for every value.
- When sources disagree keep both values and explain the methodological difference.
- If a source is inaccessible, record the item as pending with the exact data required.

## Output schema

Write `research/findings/03/03B-<task>.json` with the common fields
(`subagent_id`, `task_id`, `generated_at`, `summary`, `findings`, `data_gaps`, `assumptions`,
`challenges`, `uncertainty`, `specific`) and these subagent-specific fields inside `specific`:

- `banking_health_table`
- `credit_allocation_table`
- `inclusion_gaps`

Full JSON schema: `config/subagents.json` → `03B.output_schema`.

## Acceptance criteria

- Finding file validates against output_schema.
- Every question is answered or explicitly marked pending with the data required.
- All required specific fields are present: banking_health_table, credit_allocation_table, inclusion_gaps.
- No fact or estimate lacks an observation id.
