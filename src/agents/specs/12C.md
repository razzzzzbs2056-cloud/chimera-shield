<!-- GENERATED from config/catalog.yaml by `python -m src.cli generate`. Do not edit by hand. -->

# Subagent 12C — Tourism and Business Development Researcher

**Parent agent:** 12 — International Trade and Investment
**Depends on:** 04A, 04B, 04C, 21A, 21B, 21C, 21D, 21E

## Instructions

Study sustainable tourism, business formation, international services and regional opportunities.

Answer these research questions:

1. What are international arrivals, length of stay and spending per visitor?
2. What is travel-services export revenue in the BoP?
3. What are environmental carrying-capacity limits at major destinations?

## Evidence requirements

**Data required:**
- tourist arrivals
- travel receipts
- carrying capacity studies

**Sources (priority order; see research/sources/source_catalog.yaml):**
- nrb
- tourism_ministry
- unwto

**Rules:**
- Retrieve primary documents; record each value via ObservationStore with raw-file hash.
- Report publication date and data period for every value.
- When sources disagree keep both values and explain the methodological difference.
- If a source is inaccessible, record the item as pending with the exact data required.

## Output schema

Write `research/findings/12/12C-<task>.json` with the common fields
(`subagent_id`, `task_id`, `generated_at`, `summary`, `findings`, `data_gaps`, `assumptions`,
`challenges`, `uncertainty`, `specific`) and these subagent-specific fields inside `specific`:

- `tourism_baseline`
- `receipts_series`
- `capacity_constraints`

Full JSON schema: `config/subagents.json` → `12C.output_schema`.

## Acceptance criteria

- Finding file validates against output_schema.
- Every question is answered or explicitly marked pending with the data required.
- All required specific fields are present: tourism_baseline, receipts_series, capacity_constraints.
- No fact or estimate lacks an observation id.
