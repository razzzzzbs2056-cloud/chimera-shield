<!-- GENERATED from config/catalog.yaml by `python -m src.cli generate`. Do not edit by hand. -->

# Subagent 18C — European Union and United Kingdom Specialist

**Parent agent:** 18 — Global Power Relations
**Depends on:** 16A, 16B

## Instructions

Examine European trade regulation, investment, migration, climate requirements and development partnerships.

Answer these research questions:

1. How will EU GSP / UK DCTS treatment change after LDC graduation?
2. Which EU regulations (e.g. CBAM, due-diligence rules) affect Nepali exports?

## Evidence requirements

**Data required:**
- GSP/DCTS rules
- regulation texts

**Sources (priority order; see research/sources/source_catalog.yaml):**
- eu_trade
- uk_trade

**Rules:**
- Retrieve primary documents; record each value via ObservationStore with raw-file hash.
- Report publication date and data period for every value.
- When sources disagree keep both values and explain the methodological difference.
- If a source is inaccessible, record the item as pending with the exact data required.

## Output schema

Write `research/findings/18/18C-<task>.json` with the common fields
(`subagent_id`, `task_id`, `generated_at`, `summary`, `findings`, `data_gaps`, `assumptions`,
`challenges`, `uncertainty`, `specific`) and these subagent-specific fields inside `specific`:

- `post_graduation_preferences`
- `regulatory_exposure`

Full JSON schema: `config/subagents.json` → `18C.output_schema`.

## Acceptance criteria

- Finding file validates against output_schema.
- Every question is answered or explicitly marked pending with the data required.
- All required specific fields are present: post_graduation_preferences, regulatory_exposure.
- No fact or estimate lacks an observation id.
