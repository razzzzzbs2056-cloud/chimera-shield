<!-- GENERATED from config/catalog.yaml by `python -m src.cli generate`. Do not edit by hand. -->

# Subagent 15B — Financial and Implementation Auditor

**Parent agent:** 15 — Independent Domestic Audit
**Depends on:** 01A, 01B, 01C, 02A, 02B, 02C, 03A, 03B, 03C, 04A, 04B, 04C, 05A, 05B, 05C, 06A, 06B, 06C, 07A, 07B, 07C, 08A, 08B, 08C, 09A, 09B, 09C, 10A, 10B, 10C, 11A, 11B, 11C, 12A, 12B, 12C, 13A, 13B, 13C, 14A, 14B, 14C

## Instructions

Check budgets, financing, capacity constraints, delivery schedules and contingent liabilities.

Answer these research questions:

1. Is every investment requirement matched to an identified financing source?
2. Are delivery schedules consistent with historical execution rates?
3. Are contingent liabilities disclosed?

## Evidence requirements

**Data required:**
- strategy financing tables
- capex execution history
- debt paths

**Sources (priority order; see research/sources/source_catalog.yaml):**
- derived from other agents / platform records

**Rules:**
- Retrieve primary documents; record each value via ObservationStore with raw-file hash.
- Report publication date and data period for every value.
- When sources disagree keep both values and explain the methodological difference.
- If a source is inaccessible, record the item as pending with the exact data required.

## Output schema

Write `research/findings/15/15B-<task>.json` with the common fields
(`subagent_id`, `task_id`, `generated_at`, `summary`, `findings`, `data_gaps`, `assumptions`,
`challenges`, `uncertainty`, `specific`) and these subagent-specific fields inside `specific`:

- `financing_gap_checks`
- `schedule_realism_checks`
- `verdict`

Full JSON schema: `config/subagents.json` → `15B.output_schema`.

## Acceptance criteria

- Finding file validates against output_schema.
- Every question is answered or explicitly marked pending with the data required.
- All required specific fields are present: financing_gap_checks, schedule_realism_checks, verdict.
- No fact or estimate lacks an observation id.
