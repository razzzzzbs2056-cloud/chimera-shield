<!-- GENERATED from config/catalog.yaml by `python -m src.cli generate`. Do not edit by hand. -->

# Subagent 06A — Transport and Logistics Researcher

**Parent agent:** 06 — Infrastructure and Urban Development
**Depends on:** 05A, 05B, 05C, 13A, 13B, 13C

## Instructions

Examine roads, railways, aviation, border infrastructure and domestic and international transport costs.

Answer these research questions:

1. What is the strategic road network length and condition?
2. What are door-to-port transport time and cost to Kolkata/Haldia and Visakhapatnam?
3. What is the status of announced railway links?

## Evidence requirements

**Data required:**
- road network statistics
- freight cost per TEU
- dry-port throughput
- LPI scores

**Sources (priority order; see research/sources/source_catalog.yaml):**
- customs_nepal
- adb_nepal
- wb_lpi
- wb_npl

**Rules:**
- Retrieve primary documents; record each value via ObservationStore with raw-file hash.
- Report publication date and data period for every value.
- When sources disagree keep both values and explain the methodological difference.
- If a source is inaccessible, record the item as pending with the exact data required.

## Output schema

Write `research/findings/06/06A-<task>.json` with the common fields
(`subagent_id`, `task_id`, `generated_at`, `summary`, `findings`, `data_gaps`, `assumptions`,
`challenges`, `uncertainty`, `specific`) and these subagent-specific fields inside `specific`:

- `network_baseline`
- `trade_cost_estimates`
- `rail_project_status`

Full JSON schema: `config/subagents.json` → `06A.output_schema`.

## Acceptance criteria

- Finding file validates against output_schema.
- Every question is answered or explicitly marked pending with the data required.
- All required specific fields are present: network_baseline, trade_cost_estimates, rail_project_status.
- No fact or estimate lacks an observation id.
